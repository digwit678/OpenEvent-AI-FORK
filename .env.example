# OpenEvent-AI Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED: OpenAI API Key
# =============================================================================
# Get your key from: https://platform.openai.com/api-keys
# SECURITY: Never commit your actual API key! Use placeholder in .env.example
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# BACKEND SECURITY SETTINGS
# =============================================================================
# Comma-separated list of allowed origins for CORS (frontend URLs)
# Default: http://localhost:3000 (local dev)
# Production example: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# Enable dangerous test endpoints (NEVER set to true in production!)
# This controls access to /api/client/reset and similar destructive endpoints
ENABLE_DANGEROUS_ENDPOINTS=false

# =============================================================================
# SUPABASE INTEGRATION (Required for MVP)
# =============================================================================
# Get these from: Supabase Dashboard → Settings → API
OE_SUPABASE_URL=https://your-project-id.supabase.co
OE_SUPABASE_KEY=your-supabase-service-role-key-here

# Multi-tenant configuration
OE_TEAM_ID=your-team-uuid-here
OE_SYSTEM_USER_ID=your-system-user-uuid-here
OE_EMAIL_ACCOUNT_ID=your-email-account-uuid-here

# =============================================================================
# OPTIONAL: Development Settings
# =============================================================================
# Base agent mode: "openai", "gemini", or "stub" (testing)
AGENT_MODE=openai

# LLM profile name (from configs/llm_profiles.json)
OE_LLM_PROFILE=default

# =============================================================================
# OPTIONAL: Hybrid LLM Mode (Recommended for Cost Savings)
# =============================================================================
# Hybrid mode uses Gemini for extraction (75% cheaper) and OpenAI for drafts.
# To enable hybrid mode, add your Gemini API key below.
# Get a free key from: https://aistudio.google.com/apikey
#
# GOOGLE_API_KEY=your-gemini-api-key-here
#
# Per-operation provider overrides (defaults to AGENT_MODE if not set):
# INTENT_PROVIDER=gemini      # Intent classification
# ENTITY_PROVIDER=gemini      # Entity extraction (dates, participants, etc.)
# VERBALIZER_PROVIDER=openai  # Client-facing drafts (quality matters)

# Frontend port for local development
FRONTEND_PORT=3000
