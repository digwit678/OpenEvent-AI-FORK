is # OpenEvent-AI Environment Configuration
# ============================================================================
# IMPORTANT: NEVER commit .env files with real API keys!
# Use macOS Keychain for secure local development (see below).
# ============================================================================

# =============================================================================
# API KEYS - Use Keychain (Recommended for Local Dev)
# =============================================================================
# Store keys securely in macOS Keychain instead of .env files:
#
# 1. OpenAI Key (for verbalization):
#    security add-generic-password -s 'openevent-api-test-key' -a "$USER" -w 'sk-proj-YOUR-KEY'
#
# 2. Google/Gemini Key (for intent/entity extraction):
#    security add-generic-password -s 'openevent-gemini-key' -a "$USER" -w 'YOUR-GOOGLE-API-KEY'
#
# The dev_server.sh script automatically loads these from Keychain.
# Run: ./scripts/dev/dev_server.sh
#
# To verify keys are stored:
#    security find-generic-password -s 'openevent-api-test-key' -a "$USER" -w
#    security find-generic-password -s 'openevent-gemini-key' -w
#
# For CI/CD or production, use environment variables or secrets manager.
# =============================================================================

# If you must use .env (NOT recommended for API keys), these are the variables:
# OPENAI_API_KEY=your-openai-api-key-here
# GOOGLE_API_KEY=your-google-api-key-here

# =============================================================================
# BACKEND SECURITY SETTINGS
# =============================================================================
# Comma-separated list of allowed origins for CORS (frontend URLs)
ALLOWED_ORIGINS=http://localhost:3000

# Enable dangerous test endpoints (NEVER set to true in production!)
ENABLE_DANGEROUS_ENDPOINTS=false
can
# =============================================================================
# SUPABASE INTEGRATION (Required for MVP)
# =============================================================================
# Get these from: Supabase Dashboard -> Settings -> API
OE_SUPABASE_URL=https://your-project-id.supabase.co
OE_SUPABASE_KEY=your-supabase-service-role-key-here

# Multi-tenant configuration
OE_TEAM_ID=your-team-uuid-here
OE_SYSTEM_USER_ID=your-system-user-uuid-here
OE_EMAIL_ACCOUNT_ID=your-email-account-uuid-here

# =============================================================================
# OPTIONAL: Development Settings
# =============================================================================
# Agent mode: "gemini" (hybrid default), "openai" (fallback), "stub" (testing)
AGENT_MODE=gemini

# Detection mode: "unified" (recommended) uses both LLMs
DETECTION_MODE=unified

# LLM profile name (from configs/llm_profiles.json)
OE_LLM_PROFILE=default

# Frontend port for local development
FRONTEND_PORT=3000
