[pytest]
markers =
    integration: marks tests that hit live external services (deselect with '-m "not integration"')
addopts =
    --ignore=backend/tests
    --ignore=backend/tests_integration
    --ignore=tests/e2e
    --ignore=scripts
