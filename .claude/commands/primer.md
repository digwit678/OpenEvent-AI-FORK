---
description: Show the latest session primer (docs/daily_scrum/session_primer.md)
---

# Session Primer

Print the latest session primer (generated by the scheduled Codex automation) if present.

```bash
if [ -f docs/daily_scrum/session_primer.md ]; then
  sed -n '1,200p' docs/daily_scrum/session_primer.md
else
  echo "Missing docs/daily_scrum/session_primer.md"
  echo "Enable the Codex automation that writes this file (e.g. 'Nightly primer file')."
fi
```
