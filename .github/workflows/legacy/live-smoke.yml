# Legacy reference: relocated to `.github/workflows/legacy` to keep around without activating it.
name: live-smoke
on: workflow_dispatch
jobs:
  run:
    if: false  # disabled; retained for reference only
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}   # <-- maps secret to env var
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -e .[test]
      - run: pytest -m live_smoke -q
